# context-engine

`context-engine` builds span-backed section trees and structural indexes over Markdown documents
parsed by `context-parser`. It supports document metadata, exact section retrieval, outlines, and
deterministic fuzzy heading search.
